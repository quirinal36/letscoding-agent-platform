# 2026-08-13 로컬 합성 운영 drill

범위: 실제 production traffic·secret·WAF를 건드리지 않는 코드/계약 drill

## 실행 결과

- `pnpm check`: 세부 unit/contract/fixture/MCP protocol/Plugin E2E 전체 통과
- Plugin E2E 13개: 실제 Vite/Next build, 정책 변경 재검증, 정책 source 장애 fail-closed
- artifact validator 54개: malformed ZIP, `.env`, backslash, size/entry 한계
- MCP 58개: health/readiness, timeout/body/rate/concurrency, request ID, redaction
- policy guard unit 5개: 정상 원자적 활성화와 history 수정/삭제/rename 차단
- `pnpm audit:dependencies`: high 이상 0건이 release gate 조건
- tracked secret scan과 Plugin package validation: 통과가 release gate 조건
- local loopback deployment smoke: `/health`, `/ready`, MCP initialize와 expected revision 통과
- dependency audit: 알려진 취약점 0건

## 따라 해 본 runbook 경로

1. 정책 source 장애를 합성해 `POLICY_SERVICE_UNAVAILABLE`과 완료 보고 없음 확인
2. 작업 중 2026-08-12.1→2026-08-12.2 전환을 합성해 `REVALIDATION_REQUIRED` 후 재검증
3. 과거 history M/D/R change set을 policy guard에 넣어 각각 차단
4. 실제 Next output의 다중 파일 ZIP을 검사해 listener/stream 경계 확인

## 아직 사람 승인이 필요한 외부 drill

- 별도 Codex 계정/컴퓨터 설치와 한 문장 요청
- GitHub main ruleset/Environment reviewer가 실제로 우회·실패 merge를 차단하는지 확인
- Vercel staging→staged production→promote→Instant Rollback
- WAF 7일 Log 관찰, audit sink TTL/RBAC와 request ID 조회
- Private Vulnerability Reporting의 비공개 test report

외부 drill 증빙이 없는데 완료했다고 기록하지 않는다. production gate를 열기 전에 실행
URL/revision/policy version/승인자/rollback 결과를 이 디렉터리에 새 기록으로 추가한다.
