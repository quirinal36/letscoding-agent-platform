import "./index.js";
import { describe, expect, it } from "vitest";

describe("@letscoding/artifact-validator", () => {
  it("loads its public entry point", () => {
    expect(true).toBe(true);
  });
});
