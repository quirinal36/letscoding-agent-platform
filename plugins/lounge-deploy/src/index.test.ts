import "./index.js";
import { describe, expect, it } from "vitest";

describe("@letscoding/lounge-deploy-plugin", () => {
  it("loads its public entry point", () => {
    expect(true).toBe(true);
  });
});
