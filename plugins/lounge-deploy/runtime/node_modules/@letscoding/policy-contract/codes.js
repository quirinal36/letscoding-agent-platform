/**
 * 계약 계층 오류 코드.
 *
 * 이 코드는 **정책 자체의 결함**을 가리킨다. 사용자 작품 판정에 쓰는
 * `LD_*` 코드와는 다른 네임스페이스이며 서로 섞어 쓰지 않는다.
 * `LD_*` 카탈로그는 정책 문서의 `checks[]`가 소유한다.
 */
export const POLICY_CONTRACT_CODES = [
    /** 파일 내용이 JSON으로 파싱되지 않는다. */
    "POLICY_INVALID_JSON",
    /** JSON Schema 검증에 실패했다. */
    "POLICY_SCHEMA_INVALID",
    /** pattern은 통과했지만 달력에 없는 날짜다. */
    "POLICY_VERSION_NOT_A_CALENDAR_DATE",
    /** 압축 해제 최대 크기가 압축 최대 크기보다 작다. */
    "POLICY_ZIP_SIZE_INVERTED",
    /** `checks[]`에 같은 code가 두 번 있다. */
    "POLICY_DUPLICATE_CHECK_CODE",
    /** 다른 절이 `checks[]`에 없는 code를 참조한다. */
    "POLICY_UNKNOWN_CHECK_CODE",
    /** `frameworks[]`에 같은 key가 두 번 있다. */
    "POLICY_DUPLICATE_FRAMEWORK",
    /** `guide.path`가 `history/<version>.md` 규약과 다르다. */
    "POLICY_GUIDE_PATH_MISMATCH",
    /** 가이드 Markdown 스냅샷을 찾을 수 없다. */
    "POLICY_GUIDE_MISSING",
    /** 가이드 Markdown의 해시가 `guide.sha256`과 다르다. */
    "POLICY_GUIDE_HASH_MISMATCH",
    /** `current.json`을 찾을 수 없다. */
    "POLICY_POINTER_MISSING",
    /** `current.json`의 policyId가 요청한 정책과 다르다. */
    "POLICY_POINTER_ID_MISMATCH",
    /** 포인터가 가리키는 스냅샷 파일이 없다. */
    "POLICY_SNAPSHOT_MISSING",
    /** 스냅샷의 id가 포인터와 다르다. */
    "POLICY_SNAPSHOT_ID_MISMATCH",
    /** 스냅샷의 version이 포인터 또는 파일명과 다르다. */
    "POLICY_SNAPSHOT_VERSION_MISMATCH",
];
const CONTRACT_CODE_SET = new Set(POLICY_CONTRACT_CODES);
export function isPolicyContractCode(value) {
    return CONTRACT_CODE_SET.has(value);
}
//# sourceMappingURL=codes.js.map