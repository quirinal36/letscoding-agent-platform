/**
 * `current.json` → `history/<version>` 참조 무결성 로딩.
 *
 * 하나라도 실패하면 정책을 제공하지 않는다. 정책 조회 실패를 오래된 정책으로
 * 대체해 성공 처리하지 않는다는 운영 원칙을 계약 계층에서 강제한다.
 */
import { createHash } from "node:crypto";
import { parseCurrentPointerText, parsePolicyDocumentText } from "./parse.js";
export const CURRENT_POINTER_PATH = "current.json";
export function snapshotPath(version) {
    return `history/${version}.json`;
}
export function guidePath(version) {
    return `history/${version}.md`;
}
function failure(code, path, detail) {
    const issue = { code, path, detail };
    return { ok: false, issues: [issue] };
}
/**
 * 가이드 Markdown을 해시 이전에 정규화한다.
 *
 * Windows checkout의 CRLF와 BOM 때문에 같은 내용이 다른 해시를 갖는 일을 막는다.
 */
export function normalizeGuideText(text) {
    return text.replace(/^\uFEFF/u, "").replace(/\r\n/gu, "\n");
}
export function guideSha256(text) {
    return createHash("sha256")
        .update(normalizeGuideText(text), "utf8")
        .digest("hex");
}
async function loadSnapshot(source, policyId, version) {
    const snapshotText = await source.readText(snapshotPath(version));
    if (snapshotText === null) {
        return failure("POLICY_SNAPSHOT_MISSING", snapshotPath(version), `Snapshot for version ${version} does not exist.`);
    }
    const document = parsePolicyDocumentText(snapshotText);
    if (!document.ok) {
        return document;
    }
    if (document.value.id !== policyId) {
        return failure("POLICY_SNAPSHOT_ID_MISMATCH", "/id", `Expected ${policyId} but the snapshot declares ${document.value.id}.`);
    }
    if (document.value.version !== version) {
        return failure("POLICY_SNAPSHOT_VERSION_MISMATCH", "/version", `File name declares ${version} but the snapshot declares ${document.value.version}.`);
    }
    const guideMarkdown = await source.readText(guidePath(version));
    if (guideMarkdown === null) {
        return failure("POLICY_GUIDE_MISSING", guidePath(version), `Guide snapshot for version ${version} does not exist.`);
    }
    const actualHash = guideSha256(guideMarkdown);
    if (actualHash !== document.value.guide.sha256) {
        return failure("POLICY_GUIDE_HASH_MISMATCH", "/guide/sha256", `Expected ${document.value.guide.sha256} but the guide hashes to ${actualHash}.`);
    }
    return {
        ok: true,
        value: { document: document.value, guideMarkdown },
    };
}
/** 지정한 불변 버전의 스냅샷을 읽는다. */
export async function loadPolicyVersion(source, options) {
    return loadSnapshot(source, options.policyId, options.version);
}
/** `current.json`이 가리키는 활성 정책을 읽는다. */
export async function loadActivePolicy(source, options) {
    const pointerText = await source.readText(CURRENT_POINTER_PATH);
    if (pointerText === null) {
        return failure("POLICY_POINTER_MISSING", CURRENT_POINTER_PATH, "current.json does not exist.");
    }
    const pointer = parseCurrentPointerText(pointerText);
    if (!pointer.ok) {
        return pointer;
    }
    if (pointer.value.policyId !== options.policyId) {
        return failure("POLICY_POINTER_ID_MISMATCH", "/policyId", `Expected ${options.policyId} but current.json points at ${pointer.value.policyId}.`);
    }
    const snapshot = await loadSnapshot(source, options.policyId, pointer.value.version);
    if (!snapshot.ok) {
        return snapshot;
    }
    return {
        ok: true,
        value: { ...snapshot.value, pointer: pointer.value },
    };
}
//# sourceMappingURL=bundle.js.map